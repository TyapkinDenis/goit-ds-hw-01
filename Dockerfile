# Вказуємо базовий образ Python
FROM python:3.10

# Вказуємо робочу директорію всередині контейнера
WORKDIR /app

# Копіюємо всі файли з локальної директорії в директорію контейнера
COPY . .

# Встановлюємо залежності, якщо є (для вашого випадку залежностей немає)
RUN pip install -r requirements.txt

# Вказуємо команду для запуску програми (тут вказуємо ваш основний файл)
CMD ["python", "main.py"]